# structure/

Keep this folder aligned with the live `claude-exec` command surface and runtime contract.

- Update `INDEX.md` whenever a structure document is added, removed, renamed, or re-scoped.
- Update `cli_surface.md` when command names, aliases, flags, or examples change.
- Update `runtime_contract.md` when stdin, JSONL, runtime events, transcript replay, timeout, resume, or exit-code behavior changes.
- Update `cli_jaw_migration.md` when cli-jaw changes provider names, binary detection, environment variables, scripts, or compatibility aliases.
